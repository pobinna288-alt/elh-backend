import { ConfigService } from '@nestjs/config';
import { RedisService } from '../redis/redis.service';
import { Database } from 'better-sqlite3';
export declare class FraudService {
    private redis;
    private config;
    private db;
    private readonly logger;
    private readonly REWARD_AMOUNT;
    private readonly MAX_ADS_PER_HOUR;
    private readonly MAX_ADS_PER_DAY;
    private readonly MAX_DAILY_EARNINGS;
    private readonly MAX_WITHDRAWAL_PER_DAY;
    private readonly MIN_ACCOUNT_AGE_DAYS;
    private readonly HOLDING_PERIOD_MS;
    constructor(redis: RedisService, config: ConfigService, db: Database);
    private initializeDatabase;
    startAdSession(userId: string, adId: string): Promise<{
        sessionId: string;
        temporaryToken: string;
    }>;
    saveCheckpoint(sessionId: string, progress: number): Promise<void>;
    completeAd(sessionId: string, userId: string, data: any, ip: string, userAgent: string): Promise<{
        success: boolean;
        coinsEarned?: number;
        message: string;
        recovered?: boolean;
        withdrawableBalance?: number;
    }>;
    private grantPendingReward;
    private grantRewardWithRecovery;
    private validateSession;
    private validateTiming;
    private validateDeviceReputation;
    private validateIPReputation;
    private validateUserHistory;
    private checkDailyLimits;
    private checkVelocityLimits;
    private validateAdProvider;
    private checkRecoveryEligible;
    calculateRiskScore(userId: string): Promise<number>;
    reviewPendingRewards(): Promise<void>;
    private clearReward;
    private extendHoldingPeriod;
    private flagForManualReview;
    immediatelyBanUser(userId: string, reason: string, ip?: string): Promise<void>;
    private logFraudEvent;
    private checkCircuitBreaker;
    reconcileDailyFinances(): Promise<void>;
    getWithdrawableBalance(userId: string): Promise<number>;
    private waitForCompletion;
    private generateTemporaryToken;
    private calculateVariance;
}
