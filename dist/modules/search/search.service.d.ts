import { Repository } from 'typeorm';
import { Ad } from '../ads/entities/ad.entity';
import { RedisService } from '../redis/redis.service';
import { SearchAdsDto, SearchSuggestionsDto, TrendingSearchesDto, SearchResponseDto, SuggestionsResponseDto, TrendingResponseDto } from './dto/search.dto';
export declare class SearchService {
    private readonly adRepository;
    private readonly redisService;
    private readonly logger;
    private readonly CACHE_PREFIX;
    private readonly SEARCH_CACHE_TTL;
    private readonly SUGGESTIONS_CACHE_TTL;
    private readonly TRENDING_CACHE_TTL;
    private readonly ANALYTICS_PREFIX;
    private readonly TRENDING_KEY;
    constructor(adRepository: Repository<Ad>, redisService: RedisService);
    search(dto: SearchAdsDto, userId?: string): Promise<SearchResponseDto>;
    private executeSearch;
    private applyFullTextSearch;
    private applyFilters;
    private applySorting;
    private processResults;
    private calculateRelevanceScore;
    private highlightMatches;
    getSuggestions(dto: SearchSuggestionsDto): Promise<SuggestionsResponseDto>;
    private getCategorySuggestions;
    getTrendingSearches(dto: TrendingSearchesDto): Promise<TrendingResponseDto>;
    trackSearchClick(query: string, adId: string, userId?: string): Promise<void>;
    private normalizeQuery;
    private tokenizeQuery;
    private generateCacheKey;
    private getCachedResult;
    private cacheResult;
    private generateCursor;
    private truncateDescription;
    private escapeRegex;
    private extractFilters;
    private trackSearchAnalytics;
    private updateTrendingSearches;
    private getPopularSearchTerms;
    private getAlternativeSuggestions;
}
